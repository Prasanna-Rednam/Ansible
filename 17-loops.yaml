- name: installing packaged
  hosts: local
  connection: local
  become: yes 
  tasks:
  - name: installing packages {{item}}
    ansible.builtin.package:
      name: {{item}}
      state: present
    loop:
    - nginx
    - mysql
    - zip