- name: ansible command vs shell module
  hosts: local
  connection: local
  tasks:
  - name: command module testing
    # ansible.builtin.command: cat /etc/passwd | grep root
    ansible.builtin.shell: cat /etc/passwd | grep root
    register: users_output
    
  - name: print users_output
    ansible.builtin.debug:
      msg: "users_output: {{users_output}}"

  - name: command module testing
    ansible.builtin.shell: ls -ltr > file.txt
    register: files_output

  - name: print files_output
    ansible.builtin.debug:
      msg: "files_output: {{files_output}}"

